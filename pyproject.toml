[project]
name = "aunp-speciation"
version = "0.1.0"
description = "Monomer/dimer/trimer optical model for AuNP UV-Vis broadening"
requires-python = ">=3.9"
dependencies = ["numpy>=1.24", "scipy>=1.10", "matplotlib>=3.7"]

[tool.setuptools.packages.find]
where = ["src"]
